# CARLA Auto-Drive

### Setup
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Run
```bash
python3 main.py --visualize_detected_obstacles --visualize_lane_detection
```

Resulting videos are saved in the `visualization/` directory.
